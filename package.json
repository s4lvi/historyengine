{
    "name": "annexi",
    "version": "1.0.0",
    "scripts": {
      "start": "npm install && node server/index.js",
      "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "engines": {
      "node": ">=14.x"
    }
  }
  